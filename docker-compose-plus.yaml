---
services:
  waha:
    image: devlikeapro/waha-plus
    container_name: "waha-plus"
    env_file:
      - .env
    ports:
      - "3000:3000"
    stdin_open: true
    tty: true
    restart: unless-stopped
